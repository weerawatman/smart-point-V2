# SMART Rewards System - สรุปโครงสร้างระบบ

## 📊 โครงสร้างข้อมูล

### 1. ตาราง `users` (ผู้ใช้งานทั้งหมด)
ใช้สำหรับ **Authentication** และกำหนด **Role**

| คอลัมน์ | ประเภท | คำอธิบาย |
|---------|--------|----------|
| employee_id | text | รหัสพนักงาน (ใช้ login) |
| password | text | รหัสผ่าน |
| name | text | ชื่อ-นามสกุล |
| role | text | บทบาท: `employee` หรือ `manager` |
| avatar | text | อีโมจิสำหรับแสดงผล |

**ผู้ใช้งาน:**
- MGR001 = Manager (ผู้จัดการ สมศักดิ์)
- EMP001-EMP005 = Employees (พนักงาน)

---

### 2. ตาราง `employees` (ข้อมูลคะแนนของทุกคน)
เก็บข้อมูล**คะแนนของทุกคน** (ทั้ง Manager และ Employee)

| คอลัมน์ | ประเภท | คำอธิบาย |
|---------|--------|----------|
| employee_id | text | รหัสพนักงาน (FK → users) |
| points | integer | คะแนนปัจจุบันที่ใช้ได้ |
| total_earned | integer | คะแนนสะสมทั้งหมดที่เคยได้รับ |
| tier | text | ระดับ: Bronze/Silver/Gold |

**สำคัญ:** 
- ✅ **ทุกคนมีข้อมูลในตารางนี้** (รวม Manager ด้วย)
- ✅ **ทุกคนสามารถได้รับคะแนนได้เหมือนกัน**

---

### 3. ตาราง `allocations` (ประวัติการให้คะแนน)
บันทึกทุกครั้งที่มีการให้คะแนน

| คอลัมน์ | ประเภท | คำอธิบาย |
|---------|--------|----------|
| manager_id | text | รหัสผู้ให้คะแนน |
| manager_name | text | ชื่อผู้ให้คะแนน |
| employee_id | text | รหัสผู้รับคะแนน |
| employee_name | text | ชื่อผู้รับคะแนน |
| points | integer | จำนวนคะแนน (ตายตัว = 1) |
| reason | text | เหตุผล (ขั้นต่ำ 10 ตัวอักษร) |
| smart_value | text | ค่านิยม SMART (S/M/A/R/T) |
| created_at | timestamp | วันเวลาที่ให้คะแนน |

---

### 4. ตาราง `transactions` (ประวัติการแลกของรางวัล)
บันทึกทุกครั้งที่มีการแลกของรางวัล

| คอลัมน์ | ประเภท | คำอธิบาย |
|---------|--------|----------|
| employee_id | text | รหัสผู้แลก |
| reward_id | text | รหัสของรางวัล |
| reward_name | text | ชื่อของรางวัล |
| points | integer | คะแนนที่ใช้แลก |
| type | text | ประเภท (ตายตัว = 'redeem') |
| created_at | timestamp | วันเวลาที่แลก |

---

## 🎯 กฎการใช้งาน

### การให้คะแนน (Point Allocation)

#### Manager (ผู้จัดการ)
- ✅ **สามารถให้คะแนนได้:** สูงสุด **5 ครั้ง/เดือน**
- ✅ **ให้ครั้งละ:** 1 คะแนน (ตายตัว)
- ✅ **ต้องระบุ:**
  - ผู้รับคะแนน
  - เหตุผล (ขั้นต่ำ 10 ตัวอักษร)
  - SMART Culture Value (S/M/A/R/T)
- ✅ **สามารถให้คะแนนได้ทุกคน** (รวมถึง Manager คนอื่นด้วย)

#### Employee (พนักงาน)
- ❌ **ไม่สามารถให้คะแนนได้**
- ✅ **สามารถรับคะแนนได้**
- ✅ **สามารถแลกของรางวัลได้**

#### Admin (ผู้ดูแลระบบ)
- ✅ **สามารถเพิ่มคะแนนให้ใครก็ได้โดยตรง** ผ่าน Supabase Table Editor
- ✅ **ไม่จำกัดจำนวนครั้ง**

---

### การแลกของรางวัล (Redemption)

- ✅ **ทุกคนสามารถแลกได้** (ทั้ง Employee และ Manager)
- ✅ **ต้องมีคะแนนเพียงพอ** (`points >= reward.points`)
- ✅ **คะแนนจะถูกหัก** จาก `points` (แต่ `total_earned` ไม่เปลี่ยน)

---

### ระดับสมาชิก (Tier)

คำนวณจาก `total_earned` (คะแนนสะสมทั้งหมด):

| Tier | เงื่อนไข | ไอคอน |
|------|---------|-------|
| 🌟 Bronze | total_earned < 150 | 🌟 |
| ⭐ Silver | 150 ≤ total_earned < 300 | ⭐ |
| 👑 Gold | total_earned ≥ 300 | 👑 |

---

## 🔧 วิธีเพิ่ม Manager ลงในตาราง employees

หากคุณยังไม่ได้เพิ่ม Manager ลงในตาราง `employees` ให้รันคำสั่ง SQL นี้ใน Supabase:

```sql
INSERT INTO employees (employee_id, points, total_earned, tier) 
VALUES ('MGR001', 0, 0, 'Bronze')
ON CONFLICT (employee_id) DO NOTHING;
```

หรือใช้ไฟล์: `add-manager-to-employees.sql`

---

## 📝 ตัวอย่างการทำงาน

### ตัวอย่างที่ 1: Manager ให้คะแนน Employee

1. **ผู้จัดการ สมศักดิ์ (MGR001)** login เข้าระบบ
2. ไปที่เมนู **"ให้คะแนน"**
3. เลือก **สมชาย ใจดี (EMP001)**
4. กรอกเหตุผล: "ทำงานได้ดีมาก ช่วยทีมแก้ปัญหาได้ทันเวลา"
5. เลือก SMART Value: **T** (Think Customers)
6. กดปุ่ม **"ให้คะแนน"**

**ผลลัพธ์:**
- สมชาย ได้รับ +1 คะแนน
- `points` และ `total_earned` ของสมชายเพิ่มขึ้น 1
- ผู้จัดการเหลือโควต้า 4 ครั้งในเดือนนี้

---

### ตัวอย่างที่ 2: Employee แลกของรางวัล

1. **สมชาย ใจดี (EMP001)** มีคะแนน 150 คะแนน
2. ไปที่เมนู **"Rewards"**
3. เลือก **"หูฟังบลูทูธ Premium"** (150 คะแนน)
4. กดปุ่ม **"แลก"**
5. ยืนยันการแลก

**ผลลัพธ์:**
- `points` ของสมชาย: 150 → 0
- `total_earned` ของสมชาย: ไม่เปลี่ยน (ยังคงเป็น 250)
- `tier` ของสมชาย: ยังคงเป็น Silver

---

### ตัวอย่างที่ 3: Admin เพิ่มคะแนนโดยตรง

1. เปิด **Supabase Dashboard**
2. ไปที่ **Table Editor → employees**
3. เลือกแถวของ **MGR001**
4. แก้ไข `points` จาก 0 → 50
5. แก้ไข `total_earned` จาก 0 → 50
6. Save

**ผลลัพธ์:**
- ผู้จัดการ สมศักดิ์ ได้รับ 50 คะแนน
- สามารถแลกของรางวัลได้เหมือนพนักงานทั่วไป

---

## ✅ สรุป

1. ✅ **ทุกคนมีข้อมูลในตาราง `employees`** (รวม Manager)
2. ✅ **ทุกคนสามารถได้รับคะแนนได้เหมือนกัน**
3. ✅ **Manager ให้คะแนนได้สูงสุด 5 ครั้ง/เดือน**
4. ✅ **Employee ให้คะแนนไม่ได้**
5. ✅ **Admin เพิ่มคะแนนได้ไม่จำกัดผ่าน Supabase**
6. ✅ **ข้อมูล sync กันทุกอุปกรณ์ผ่าน Supabase**
