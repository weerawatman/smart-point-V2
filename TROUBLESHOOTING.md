# 🔧 แก้ไข Build Error บน Cloudflare Pages

## ปัญหา
Cloudflare กำลังใช้ Build command ที่ผิด: `npx wrangler deploy`  
แต่โปรเจกต์นี้เป็น **Static Site** ไม่ต้องการ build command

## ✅ วิธีแก้ไข (ทำตามขั้นตอน)

### ขั้นตอนที่ 1: เข้าสู่ Build Settings

1. ไปที่ [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. เลือก **Pages** จากเมนูซ้าย
3. คลิกที่โปรเจกต์ของคุณ (smart-point-V2)
4. คลิกแท็บ **Settings** (ด้านบน)
5. เลื่อนลงไปหา **Build & deployments**
6. ในส่วน **Build configuration** คลิกปุ่ม **Edit configuration**

### ขั้นตอนที่ 2: แก้ไข Build Settings

แก้ไขค่าต่อไปนี้:

| Field | ค่าที่ต้องตั้ง |
|-------|---------------|
| **Framework preset** | `None` |
| **Build command** | **(ลบออกให้เป็นค่าว่าง)** |
| **Build output directory** | `/` |
| **Root directory** | **(เว้นว่าง)** |

> ⚠️ **สำคัญ**: Build command ต้อง**ว่างเปล่า** อย่ามีอะไรเลย!

### ขั้นตอนที่ 3: บันทึกและ Deploy ใหม่

1. คลิกปุ่ม **Save** (สีน้ำเงิน)
2. กลับไปที่แท็บ **Deployments**
3. คลิกปุ่ม **Retry deployment** (หรือ **Create new deployment**)

### ขั้นตอนที่ 4: รอ Deploy สำเร็จ

- รอประมาณ 30 วินาที - 1 นาที
- ดูสถานะใน Deployments tab
- เมื่อเห็น ✅ **Success** = เสร็จสมบูรณ์!

---

## 🌐 ทดสอบแอปพลิเคชัน

เมื่อ deploy สำเร็จ:

1. คลิกที่ URL ที่ได้ (เช่น `https://smart-point-v2.pages.dev`)
2. ทดสอบ Login:
   - **Manager**: รหัส `MGR001` / รหัสผ่าน `123456`
   - **Employee**: รหัส `EMP001` / รหัสผ่าน `123456`

---

## ❓ FAQ

### Q: ทำไมต้องลบ Build command?
**A:** เพราะโปรเจกต์นี้เป็น Static HTML/CSS/JS ไม่ต้อง build อะไร ใช้ไฟล์ได้เลย

### Q: ถ้ายัง Error อยู่?
**A:** ตรวจสอบว่า:
- Build command **ว่างเปล่าจริงๆ** (ไม่มีช่องว่างหรืออักขระใดๆ)
- Build output directory เป็น `/` (slash เดียว)
- Framework preset เป็น `None`

### Q: จะอัปเดตโค้ดยังไง?
**A:** แก้ไขไฟล์ในเครื่อง แล้วรัน:
```powershell
git add .
git commit -m "Update: คำอธิบาย"
git push
```
Cloudflare จะ auto-deploy ให้อัตโนมัติ!

---

## 📞 ยังมีปัญหา?

หากทำตามแล้วยังไม่ได้ ให้:
1. ส่ง screenshot ของ Build settings มาให้ดู
2. ส่ง Build log (ข้อความ error) มาให้ดู
3. ตรวจสอบว่าไฟล์ `index.html` อยู่ใน root directory ของ repo
